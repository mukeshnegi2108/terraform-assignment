# Backend configuration for development environment

bucket         = "terraform-state-webapp-dev"
key            = "applications/webapp/dev/terraform.tfstate"
region         = "us-east-1"
dynamodb_table = "terraform-state-lock-dev"
encrypt        = true

# Optional: Use specific AWS profile
# profile = "dev-profile"